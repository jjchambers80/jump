# Playwright specs skipped by CI (.github/workflows/e2e.yml sets E2E_QUARANTINE=1).
# They failed on 2026-09-24, the first time the suite ever ran in CI: stale selectors,
# unmocked endpoints and specs that never sign in. Fix a spec, delete its line, and CI
# guards it from then on. Never add a line to silence a new failure — fix it.
# Tracked on the Kanban board: t_7bd3fe04.