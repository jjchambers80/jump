{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm install"]
    },
    "build": {
      "commands": ["cd packages/db && npx prisma generate"]
    }
  },
  "deploy": {
    "startCommand": "cd packages/db && npx prisma migrate deploy && cd ../../backend && node src/api/server.js"
  }
}
